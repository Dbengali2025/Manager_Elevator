# Ralph Progress Log
Started: 2026-02-23
Feature: Manager Elevator MVP - Full Product Build

## Codebase Patterns
- AppLayout at `src/components/layout/AppLayout.tsx` — wraps all `(dashboard)` routes
- Route group `app/(dashboard)/layout.tsx` applies AppLayout to all authenticated pages
- Sidebar nav items defined in `navigation` array in AppLayout — add new routes there
- Mobile sidebar uses Headless UI Dialog + Transition (breakpoint: md/768px)
- Insforge client at `src/lib/insforge.ts` — use `insforgeClient.from("table")` for REST, `insforgeAuth` for auth
- All Insforge methods return `{ data, error }` — always check error first
- PostgREST query syntax for filtering: `"?column=eq.value&other=gt.5"`
- Dashboard page is a Server Component — `getDashboardData()` fetches user name + progress records
- Dashboard components in `src/components/dashboard/` — import as `@/components/dashboard/ComponentName`
- Database types at `src/db/types.ts` — import `User`, `UserProgress`, `Milestone`, `STAGE_ORDER`
- SQL migrations in `src/db/migrations/` — named `NNN_description.sql`, use `IF NOT EXISTS`
- Headless UI v2 Tab: use `data-[selected]` for selected styling, wrap `useSearchParams()` in `<Suspense>`
- Tracker tab components at `src/components/trackers/` — each manages own data fetching
- Standalone scripts in `src/scripts/` — run with `npx tsx --env-file=.env.local` (no dotenv needed)
- Embeddings: `insforgeEmbeddings.create({ input })` in `src/lib/insforge.ts` — 1536-dim vectors for pgvector
- Chat actions at `src/actions/chat.ts` — CRUD for conversations and messages tables
- CI Professor page at `src/app/(dashboard)/ci-professor/page.tsx` — full chat UI with history, starter questions, typing indicator
- RAG chat API at `src/app/api/chat/route.ts` — streaming SSE endpoint for AI responses with book context
- RPC function `match_book_embeddings` for pgvector semantic search — migration 004
- Streaming pattern: fetch → ReadableStream reader → parse SSE `data:` lines → accumulate content

## Key References
- PRD: /tasks/prd-manager-elevator-mvp.md
- Design System: /Manager_Elevator_Design_System.pptx.pdf
- Mockups: /public/main-dashboard-mockup.png, /public/CI-done-right-success-dashboard-mockup.png, /public/CI-done-right-trackers-mockup.png
- Book (RAG source): /Dana Thompson_Bulletproof Your Manager Career_20260105.md
- TGE Offerings (lesson titles): /TGE Offerings.md
- Logo: /public/manager-elevator_logo.png
- Insforge API Base: https://97k43jb9.us-west.insforge.app

## Design System Quick Ref
- Navy: #08376B | Sky Blue: #35C0ED | Mint Green: #9AEBA6 | Teal: #2F90B0
- Off White: #F5F7FA | Pale Gray: #E8ECF0 | Charcoal: #1C2733
- Success: #2E7D4F | Warning: #D4940A | Error: #C53030
- Fonts: Tenor Sans (headings), Montserrat (body)
- Type: Display 36px, H1 28px, H2 22px, H3 16px, Body 14px, Caption 12px
- Spacing: xs 4px, sm 8px, md 16px, lg 24px, xl 32px, 2xl 48px, 3xl 64px
- Radius: sm 6px, md 12px, lg 16px
- Grid: 12-col, 8px base, 24px gutters
- Breakpoints: 640px, 768px, 1024px, 1280px

---

## 2026-02-23 - US-001
Session: Initial setup
- Scaffolded Next.js 14 with App Router, TypeScript, Tailwind CSS
- Configured Tailwind with all design system tokens (colors, spacing, radius, fonts, typography)
- Loaded Google Fonts: Tenor Sans (headings) and Montserrat (body) via next/font/google
- Installed Headless UI (@headlessui/react)
- Set up base layout.tsx with font CSS variables and font-body default
- Added typecheck script to package.json
- Removed default Geist fonts and boilerplate page
- Created root AGENTS.md with project patterns
- Files changed: package.json, tailwind.config.ts, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, AGENTS.md
- **Learnings for future iterations:**
  - Tenor Sans only has weight 400 — must specify `weight: "400"` explicitly
  - Project dir has space ("Manager Elevator") — always quote paths
  - next/font/google uses CSS variables (--font-montserrat, --font-tenor-sans) applied via Tailwind fontFamily config
  - `font-body` class applies Montserrat, `font-heading` applies Tenor Sans
---

## 2026-02-23 - US-002
Session: Insforge client setup
- Created `.env.example` with placeholder values for INSFORGE_URL, INSFORGE_API_KEY, DANA_NOTIFICATION_EMAIL
- `.gitignore` already covers `.env*.local` — no changes needed
- Created `src/lib/insforge.ts` with:
  - TypeScript types: InsforgeResponse<T>, InsforgeError, AuthTokens, UserRecord, Row
  - `insforgeClient` — REST client with `.from(table).select/insert/update/delete`
  - `insforgeAuth` — auth helpers for signup, login, refreshToken, getUser, resetPassword, updatePassword, verifyOtp
- Created `src/lib/AGENTS.md` with import patterns and usage notes
- Updated root `AGENTS.md` with Insforge client section
- Files changed: .env.example, src/lib/insforge.ts, src/lib/AGENTS.md, AGENTS.md
- **Learnings for future iterations:**
  - Insforge uses PostgREST-style query syntax for filtering (e.g. `?id=eq.abc`)
  - `INSFORGE_API_KEY` is server-only; `NEXT_PUBLIC_INSFORGE_URL` is client-safe
  - All auth endpoints return `AuthTokens` with `access_token`, `refresh_token`, `expires_in`
  - Use `Prefer: return=representation` header to get the inserted/updated row back
---

## 2026-02-23 - US-003
Session: Core database schema
- Created SQL migration at `src/db/migrations/001_core_tables.sql` with:
  - `users` table: id, email, full_name, company_name, industry, role_title, onboarding_completed, ci_experience_level, miestro_linked, role, created_at, updated_at
  - `user_progress` table: id, user_id (FK), stage (enum), status (enum), completed_at, created_at — UNIQUE(user_id, stage)
  - `milestones` table: id, user_id (FK), milestone_type (enum), unlocked, unlocked_at, created_at — UNIQUE(user_id, milestone_type)
  - RLS policies: users read/write own rows, admin can read all
  - Auto-update trigger for `users.updated_at`
  - Indexes on foreign keys, email, role, and stage
- Created `src/db/types.ts` with TypeScript interfaces: User, UserProgress, Milestone, and insert types
- Exported `STAGE_ORDER` constant for ordered progression logic
- Created `src/db/AGENTS.md` with database patterns
- Updated root `AGENTS.md` with database section
- Files changed: src/db/migrations/001_core_tables.sql, src/db/types.ts, src/db/AGENTS.md, AGENTS.md
- **Learnings for future iterations:**
  - Use CHECK constraints for enum-like columns in PostgreSQL (not actual ENUM types) — easier to extend later
  - UNIQUE(user_id, stage) prevents duplicate progress rows per stage
  - `auth.uid()` is used in RLS policies to reference the authenticated user's UUID
  - CASCADE delete on FK means deleting a user cleans up their progress and milestones
---

## 2026-02-23 - US-004
Session: Tracker and chat database schema
- Created SQL migration at `src/db/migrations/002_tracker_tables.sql` with:
  - `improvement_opportunities` table: CRUD for workplace waste tracking, priority/status filters, updated_at trigger
  - `war_battle_sessions` table: 14-week battle tracking with battle_number (1-3), week_number (1-14), powerpoint_link, status
  - `winning_solutions` table: before/after metrics with metric_type enum, NUMERIC values for calculations
  - `success_nuggets` table: achievement statements with source (manual/ai_generated), updated_at trigger
  - `conversations` table: CI Professor chat threads
  - `messages` table: chat messages with role (user/assistant), FK to conversations with CASCADE
  - `book_embeddings` table: pgvector(1536) for RAG, HNSW index for cosine similarity
  - RLS policies: all tables — users CRUD own rows, admin can read all
  - Messages RLS uses join-based policy checking parent conversation's user_id
  - Book embeddings: read for all authenticated users, insert restricted to admin
  - Indexes on all foreign keys, status/priority columns, and HNSW on embeddings
- Updated `src/db/types.ts` with new interfaces and insert types:
  - ImprovementOpportunity, WarBattleSession, WinningSolution, SuccessNugget
  - Conversation, Message, BookEmbedding
  - All corresponding Insert types and union types for enums
- Updated `src/db/AGENTS.md` with tracker, chat, and embeddings sections
- Updated root `AGENTS.md` with new import patterns
- Files changed: src/db/migrations/002_tracker_tables.sql, src/db/types.ts, src/db/AGENTS.md, AGENTS.md
- **Learnings for future iterations:**
  - pgvector uses `vector(1536)` type — 1536 dimensions matches OpenAI embedding models
  - HNSW index with `vector_cosine_ops` is preferred for cosine similarity search (faster than IVFFlat)
  - Messages RLS requires join to conversations table — use EXISTS subquery pattern
  - Book embeddings have no user_id — shared resource, authenticated read, admin-only write
  - `NUMERIC` type used for before/after values (not FLOAT) to avoid floating point precision issues
  - Reuse `update_updated_at_column()` trigger function from migration 001 — no need to recreate
---

## 2026-02-23 - US-005
Session: Sidebar navigation and app layout shell
- Created `src/components/layout/AppLayout.tsx` — client component with:
  - Left sidebar (240px, navy bg) with logo, 5 nav items, user avatar at bottom
  - Top header bar (64px) with notification bell and settings gear icons
  - Mobile responsive: sidebar collapses to hamburger menu below 768px using Headless UI Dialog + Transition
  - Active route highlighting via `usePathname()` comparison
  - SVG icons for all nav items (Dashboard, Masterclass, CI Trackers, Success Dashboard, CI Professor)
- Created `src/app/(dashboard)/layout.tsx` — wraps all authenticated routes with AppLayout
- Created `src/app/(dashboard)/dashboard/page.tsx` — placeholder dashboard page
- Created directory structure for all dashboard routes: masterclass, trackers, success-dashboard, ci-professor, settings, onboarding, admin
- Created `src/components/layout/AGENTS.md` with component patterns
- Updated root `AGENTS.md` with App Layout section
- Files changed: src/components/layout/AppLayout.tsx, src/app/(dashboard)/layout.tsx, src/app/(dashboard)/dashboard/page.tsx, src/components/layout/AGENTS.md, AGENTS.md
- **Learnings for future iterations:**
  - Headless UI v2 uses `Dialog` + `Transition` pattern for animated overlays — wrap Dialog in Transition.Show
  - `usePathname()` returns the path without query params — good for nav highlighting
  - Use `pathname.startsWith(item.href + "/")` to handle nested route active states
  - SVG icons as inline React components keep co-location simple — no icon library needed
  - `flex-shrink-0` on sidebar prevents it from shrinking when content is wide
  - `overflow-hidden` on root container prevents double scrollbars — only content area scrolls
---

## 2026-02-23 - US-006
Session: Landing page (public)
- Replaced placeholder `src/app/page.tsx` with full landing page
- Sections: sticky header, hero (navy-to-teal gradient), value props (4 cards), features list with milestone previews, pricing (monthly $97 / annual $1,000), CTA, footer
- Hero headline: "Finally Execute Continuous Improvement the Right Way — Without the Expensive Consultant."
- Subheadline about practical step-by-step guidance for Black middle managers
- 4 value prop cards: Practical Implementation, 14-Week Methodology, Career Advancement, Affordable Access
- Pricing section with monthly and annual plans, "Best Value" badge on annual
- Primary CTA: "Start Your Journey" linking to /signup throughout
- Manager Elevator logo in header and footer via next/image
- TGE brand colors throughout (navy, sky blue, mint green, teal)
- Mobile-responsive with Tailwind sm/lg breakpoints
- Tenor Sans for headings, Montserrat for body text
- Updated root AGENTS.md with Landing Page section
- Files changed: src/app/page.tsx, AGENTS.md
- **Learnings for future iterations:**
  - Landing page is a Server Component (no `"use client"`) — keeps JS bundle small (186 B route size)
  - Use `next/image` with `priority` for above-the-fold images (logo in header)
  - Gradient backgrounds: `bg-gradient-to-br from-navy via-navy to-teal` for hero sections
  - Decorative blur circles: absolute positioned divs with `blur-3xl` and low opacity for subtle background effects
  - In-page anchor links work with `id="pricing"` and `href="#pricing"`
  - SVG icons inline as React components avoid external dependencies
---

## 2026-02-23 - US-007
Session: Registration page with Insforge Auth
- Created auth route group `app/(auth)/` with centered layout (no sidebar)
- Created `app/(auth)/layout.tsx` — offWhite bg, flex-center
- Created `app/(auth)/signup/page.tsx` — client component with two-step flow:
  - Step 1: Signup form (full name, email, password, company, industry dropdown, role/title)
  - Step 2: Email verification code input
- Password validation: 8+ chars, 1 uppercase, 1 number — inline check/x icons
- Created `src/actions/auth.ts` with server actions:
  - `signupAction()` — calls Insforge Auth signup + creates user profile in users table
  - `verifyOtpAction()` — verifies email OTP, sets httpOnly JWT cookies
- Duplicate email detection with user-friendly error message
- After successful verification, redirects to /onboarding
- Form uses design system: navy buttons, md border-radius inputs, Montserrat font
- Created `src/actions/AGENTS.md` with action patterns
- Updated root `AGENTS.md` with Auth Pages section
- Files changed: src/app/(auth)/layout.tsx, src/app/(auth)/signup/page.tsx, src/actions/auth.ts, src/actions/AGENTS.md, AGENTS.md
- **Learnings for future iterations:**
  - Auth route group `(auth)` is separate from `(dashboard)` — no AppLayout wrapping
  - `cookies()` in Next.js 14 must be awaited: `const cookieStore = await cookies()`
  - Server actions return `{ success, error }` — can't do server-side redirect, client handles navigation
  - Input styling pattern: `border-paleGray rounded-md focus:ring-skyBlue focus:border-skyBlue`
  - Button styling pattern: `bg-navy text-white rounded-md hover:bg-navy/90`
  - Industry dropdown uses `<select>` with `required` + disabled empty `<option>` for placeholder
---

## 2026-02-23 - US-008
Session: Login page, password reset, and auth middleware
- Created `src/app/(auth)/login/page.tsx` — client component with:
  - Email + password fields with submit button
  - "Forgot Password?" link to /reset-password
  - Successful login redirects to /dashboard via `loginAction()`
  - "Don't have an account?" link to /signup
- Created `src/app/(auth)/reset-password/page.tsx` — client component with three-step flow:
  - Step 1: Enter email → calls `requestPasswordResetAction()`
  - Step 2: Enter code + new password + confirm password → calls `confirmPasswordResetAction()`
  - Step 3: Success confirmation with "Go to Login" button
  - Password validation with inline check/x icons (same pattern as signup)
  - Passwords must match validation
- Added server actions to `src/actions/auth.ts`:
  - `loginAction(email, password)` — authenticates via Insforge Auth, sets JWT cookies
  - `requestPasswordResetAction(email)` — sends recovery code, always returns success (prevents email enumeration)
  - `confirmPasswordResetAction(email, code, newPassword)` — verifies recovery OTP, updates password
  - `logoutAction()` — deletes auth cookies
- Created `src/middleware.ts` — auth middleware:
  - Public routes: `/`, `/login`, `/signup`, `/reset-password`
  - Auth-only routes redirect to `/dashboard` if already logged in
  - Protected routes redirect to `/login?redirect={path}` if no auth token
  - Skips static files, API routes, images
  - If only refresh_token exists, request passes through for server-side refresh
- Updated AGENTS.md (root + actions)
- Files changed: src/app/(auth)/login/page.tsx, src/app/(auth)/reset-password/page.tsx, src/middleware.ts, src/actions/auth.ts, AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Middleware matcher regex: `"/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)"` excludes static assets
  - `requestPasswordResetAction()` always returns success to prevent email enumeration — important security pattern
  - Password reset uses `verifyOtp(email, code, "recovery")` type — different from signup's `"signup"` type
  - Login redirect parameter: `loginUrl.searchParams.set("redirect", pathname)` — can use this later for post-login redirect
  - Auth-only routes (login/signup/reset) redirect authenticated users to dashboard to prevent confusion
---

## 2026-02-23 - US-009
Session: Onboarding flow - welcome and assessment steps
- Created `src/actions/onboarding.ts` with server actions:
  - `getOnboardingUserInfo()` — fetches user name, onboarding state, current step from users table
  - `saveOnboardingStep(step)` — persists current wizard step for resume support
  - `saveAssessmentAnswers(answers)` — saves CI assessment, derives experience level (beginner/intermediate/advanced)
  - `completeOnboarding()` — marks onboarding complete, creates user_progress record
  - Helper `getToken()` — reads access_token from cookies
  - Helper `deriveExperienceLevel(answers)` — scores 3 assessment questions
- Created `src/app/(dashboard)/onboarding/page.tsx` — client component with 5-step wizard:
  - Step 1: Welcome message with user's first name, overview of what's ahead
  - Step 2: CI experience assessment — 3 multiple-choice questions about Lean Six Sigma, CI event experience, metrics tracking
  - Step 3: Feature tour — 5 cards showing Masterclass, CI Trackers, Success Dashboard, CI Professor, Success Nuggets
  - Step 4: Miestro account link — instructions and "Go to Miestro" button (placeholder URL)
  - Step 5: Dashboard preview — journey progress bar, milestone card previews
- Progress indicator: "Step X of 5" with segmented bar
- "Skip onboarding" text link available on every step
- Resume support: step saved per change, loaded on page visit
- Already-completed users redirected to /dashboard
- Created SQL migration `src/db/migrations/003_add_onboarding_step.sql` — adds `onboarding_step INTEGER DEFAULT 1` to users table
- Updated `src/db/types.ts` — added `onboarding_step: number` to User interface
- Updated AGENTS.md (root + actions + db)
- Files changed: src/actions/onboarding.ts, src/app/(dashboard)/onboarding/page.tsx, src/db/migrations/003_add_onboarding_step.sql, src/db/types.ts, AGENTS.md, src/actions/AGENTS.md, src/db/AGENTS.md
- **Learnings for future iterations:**
  - Onboarding page is inside `(dashboard)` route group — it gets the AppLayout sidebar wrapper
  - Server actions use `insforgeAuth.getUser(token)` to get auth user ID, then query users table for profile data
  - `saveOnboardingStep` is fire-and-forget (.catch(console.error)) — don't block UI navigation
  - Assessment derives experience level: score 0 = beginner, 1-2 = intermediate, 3 = advanced
  - User type updated to include `onboarding_step` field — migration 003 adds this column
  - Onboarding uses emojis (&#127775; &#128737;) for milestone card previews — rendered as HTML entities
---

## 2026-02-23 - US-010
Session: Main dashboard - journey progress bar
- Created `src/actions/dashboard.ts` with `getDashboardData()` server action:
  - Fetches user name from users table (for welcome header)
  - Fetches all user_progress records (for journey progress bar)
  - Returns `{ success, data: { userName, progressRecords } }`
- Created `src/components/dashboard/JourneyProgressBar.tsx` — client component:
  - 8-stage progress visualization: Onboarding → Module 1-4 → Battle 1-3
  - Stage status logic: completed (filled sky-blue + checkmark), active (sky-blue border + number/trophy), future (gray border)
  - Battle stages show trophy icon instead of number
  - Connector lines: solid sky-blue for completed, dashed gray for future
  - Horizontally scrollable on mobile (min-width 600px)
- Updated `src/app/(dashboard)/dashboard/page.tsx`:
  - Converted to Server Component (async, calls getDashboardData())
  - Welcome header with user's first name
  - JourneyProgressBar with progress records
- Created `src/components/dashboard/AGENTS.md`
- Updated root AGENTS.md and src/actions/AGENTS.md
- Files changed: src/actions/dashboard.ts, src/components/dashboard/JourneyProgressBar.tsx, src/components/dashboard/AGENTS.md, src/app/(dashboard)/dashboard/page.tsx, AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Dashboard page is a Server Component (async function) — data fetching happens server-side, no "use client"
  - JourneyProgressBar is a client component (needs no interactivity but imports are simpler this way)
  - `getStageStatus` determines active stage by checking if all prior stages are completed — doesn't require an explicit "in_progress" record
  - `contents` CSS class lets flex items render flat without an extra wrapper div
  - Connector line offset uses `mt-[-12px]` to align with circle centers vertically
---

## 2026-02-23 - US-011
Session: Dashboard stat cards and feature navigation cards
- Expanded `getDashboardData()` server action to fetch:
  - `onboarding_completed` from users table
  - `sessionsCompleted` count from war_battle_sessions (status=done)
  - `activeProjects` count from improvement_opportunities (status=active)
  - `milestones` from milestones table (for badges earned)
- Created `src/components/dashboard/StatCards.tsx` — 4 stat cards:
  - Current Module (derived from progress), Sessions Done, Active Projects, Badges Earned
  - 2x2 grid on mobile, 4 across on desktop
  - White bg, rounded-lg, shadow-sm, large number display
- Created `src/components/dashboard/FeatureNavigationCards.tsx` — 5 feature nav cards:
  - Onboarding, Masterclass, CI Trackers, Success Dashboard, CI Professor
  - Status tags: Complete (green), In Progress (blue), Up Next (orange), Locked (gray)
  - Feature unlock rules per acceptance criteria
  - Locked cards disabled with lock icon and opacity-60
  - Unlocked cards navigate via router.push()
  - Top border accent for complete (green) and in_progress (blue)
  - Responsive grid: 1 col mobile → 2 cols tablet → 5 cols desktop
- Updated dashboard page to render all three sections: progress bar, stat cards, feature cards
- Updated AGENTS.md (root, dashboard components, actions)
- Files changed: src/actions/dashboard.ts, src/components/dashboard/StatCards.tsx, src/components/dashboard/FeatureNavigationCards.tsx, src/app/(dashboard)/dashboard/page.tsx, src/components/dashboard/AGENTS.md, src/actions/AGENTS.md, AGENTS.md
- **Learnings for future iterations:**
  - `DashboardData` type expanded — other dashboard components should use this same server action
  - Feature unlock logic is per-card via `getStatus()` callback — easy to modify rules per feature
  - Status config map pattern: define all visual variants in a const object, look up by status key
  - Insforge `select` with `&select=id` returns minimal data — good for count queries
  - Milestones filtered client-side with `.filter(m => m.unlocked).length` for badge count
  - SVG icons inline as React components — icon per feature card with matching brand colors
---

## 2026-02-23 - US-012
Session: Masterclass access page with module overview and session checklist
- Created `src/actions/masterclass.ts` with server actions:
  - `getMasterclassData()` — fetches user name, progress records, and WAR battle sessions
  - `toggleSessionCompletion(week, name, battle, done)` — toggles session done/pending, auto-updates battle progress
  - `updateModuleProgress(userId, token)` — internal helper that syncs battle stage progress
  - Exports `MODULES` constant (4 modules with lesson title arrays from TGE Offerings)
  - Exports `WAR_BATTLE_SESSIONS` constant (14 session definitions with week/name/battle)
- Created `src/app/(dashboard)/masterclass/page.tsx` — client component with:
  - Page title: "Leading Bulletproof Continuous Improvement Masterclass"
  - "Go to Miestro" CTA button (placeholder URL, opens in new tab)
  - Current progress indicator showing module number and sessions completed count
  - 4 expandable module cards with lesson titles (current module auto-expanded)
  - Module status badges: Complete (green), In Progress (blue), Not Started (gray)
  - 14-week session checklist with checkboxes, battle badges, and date completed
  - Optimistic UI updates on checkbox toggle with server sync
  - Downloads section placeholder ("Tools coming soon")
- Updated root AGENTS.md with Masterclass section
- Updated src/actions/AGENTS.md with Masterclass Actions section
- Files changed: src/actions/masterclass.ts, src/app/(dashboard)/masterclass/page.tsx, AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Masterclass page is a client component (needs state for toggling sessions, expanding modules)
  - WAR_BATTLE_SESSIONS maps weeks to battle numbers: battle 1 = weeks 1-6, battle 2 = weeks 7-12, battle 3 = weeks 13-14
  - Module cards use useState for expand/collapse — isCurrentModule prop sets initial open state
  - Optimistic updates: update local state immediately, then fire server action, reload on completion
  - `toggleSessionCompletion` both creates/updates session records AND updates battle progress in user_progress
  - The 14 sessions in the checklist are the same as the WAR Battle sessions tracked in the tracker
---

## 2026-02-23 - US-013
Session: Tracker hub with tabbed navigation shell
- Created `src/app/(dashboard)/trackers/page.tsx` — client component with:
  - Page title: "CI Done Right Trackers" with subtitle
  - 4 horizontal tabs using Headless UI v2 TabGroup: Waste WAR Battles, Opportunities, Impact Tracker, Success Nuggets
  - Active tab shows sky blue underline indicator (3px)
  - URL syncs with tab via `?tab=<key>` query param using `useSearchParams()` + `router.replace()`
  - Default tab: Waste WAR Battles (index 0)
  - Mobile responsive: tabs scroll horizontally via `overflow-x-auto` container
  - Each tab renders a styled placeholder component with icon, title, and description
- Wrapped page content in `<Suspense>` boundary (required by `useSearchParams()`)
- Fixed pre-existing lint issue: removed unused `doneWeeks` variable in `masterclass.ts`
- Created `src/app/(dashboard)/trackers/AGENTS.md` with tab patterns and component mapping
- Updated root `AGENTS.md` with CI Trackers section
- Files changed: src/app/(dashboard)/trackers/page.tsx, src/app/(dashboard)/trackers/AGENTS.md, AGENTS.md, src/actions/masterclass.ts
- **Learnings for future iterations:**
  - Headless UI v2 Tab uses `data-[selected]` attribute for styling (not className render props or `aria-selected`)
  - `useSearchParams()` in Next.js 14 requires the component to be wrapped in `<Suspense>` — otherwise build fails with static generation
  - Tab underline pattern: use `after:` pseudo-element with `data-[selected]:after:bg-skyBlue` — clean CSS-only approach
  - `router.replace()` with `{ scroll: false }` prevents page scroll on tab change
  - Tab content components should manage their own data fetching — tab shell passes no props
---

## 2026-02-23 - US-014
Session: Waste WAR Battle project tracker table
- Created `src/actions/trackers.ts` with server actions:
  - `getWarBattleSessions()` — fetches all WAR battle session records for current user
  - `markSessionComplete(weekNumber)` — marks session as done with current timestamp, creates record if none exists
  - `saveSessionLink(weekNumber, link)` — saves/updates PowerPoint link for a session
- Created `src/components/trackers/WarBattleTracker.tsx` — client component with:
  - 14-row session table with columns: Week #, Session Name, PowerPoint Link, Date Completed, Status, Action
  - Pre-populated rows merged from `WAR_BATTLE_SESSIONS` constants with DB records
  - Active session logic: first non-done session is marked active (only one at a time)
  - Status badges: Done (green), Active (blue), Pending (gray) via `STATUS_CONFIG` map
  - "Mark Complete" button on active row with loading spinner state
  - Date auto-fills with current date on completion
  - PowerPoint Link inline editing: click "No file yet" → text input → Save/Cancel
  - Filled links show as clickable with edit and external link icons
  - Battle number badge on sessions from battles 2 and 3
  - Progress summary bar showing X of 14 sessions completed
  - "How It Works" info box at bottom explaining the workflow
  - Loading skeleton while fetching data
- Updated `src/app/(dashboard)/trackers/page.tsx`:
  - Imported `WarBattleTracker` component
  - Replaced `WarBattlesPlaceholder` in first `TabPanel`
  - Removed unused `WarBattlesPlaceholder` function
- Created `src/components/trackers/AGENTS.md` with tracker component patterns
- Updated `src/actions/AGENTS.md` with Tracker Actions section
- Updated root `AGENTS.md` with Tracker Components section
- Files changed: src/actions/trackers.ts, src/components/trackers/WarBattleTracker.tsx, src/components/trackers/AGENTS.md, src/app/(dashboard)/trackers/page.tsx, AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Tracker components manage their own data — fetch on mount via `useEffect` + `useCallback`, re-fetch after mutations
  - `WAR_BATTLE_SESSIONS` from masterclass.ts is the single source of truth for session definitions
  - Active session is computed client-side: find first row where status !== "done"
  - LinkCell uses local editing state — saves on Enter key or Save button, reverts on Escape or Cancel
  - Use `useCallback` for fetch functions passed to `useEffect` to avoid unnecessary re-renders
  - Status config map pattern: `Record<StatusType, { label, bg, text }>` keeps badge styling consistent and DRY
---

## 2026-02-23 - US-015
Session: Email notification to Dana on session completion
- Added `insforgeEmail.send()` helper to `src/lib/insforge.ts` — sends via Insforge Email (AWS SES) endpoint `/email/v1/send`
- Created `src/actions/notifications.ts` with 3 notification functions:
  - `notifySessionCompleted()` — WAR Battle session completed (user name, company, session #, name, date, PPT link)
  - `notifyMilestoneUnlocked()` — Milestone unlocked (waste_eliminator or ci_consultant)
  - `notifyNewUserRegistered()` — New user registration (all user details)
- `emailTemplate()` helper builds branded HTML email with navy header, logo, white content, TGE footer
- Integrated into `markSessionComplete()` in trackers.ts — fire-and-forget via `.then()` + `.catch()`
  - Fetches user name/company from users table to include in email
- Integrated into `signupAction()` in auth.ts — fire-and-forget via `.catch()`
- `notifyMilestoneUnlocked()` exported and ready to be called from success dashboard milestone unlock logic (US-020)
- Dana's email configurable via `DANA_NOTIFICATION_EMAIL` env var (already in .env.example)
- Updated AGENTS.md (root, src/lib, src/actions) with notification patterns
- Files changed: src/lib/insforge.ts, src/actions/notifications.ts, src/actions/trackers.ts, src/actions/auth.ts, AGENTS.md, src/lib/AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Fire-and-forget pattern: use `.catch(err => console.error(...))` — never `await` notification calls in user-facing flows
  - Insforge Email endpoint: POST `/email/v1/send` with `{ to, subject, html }` body
  - HTML email template uses inline styles (no external CSS support in email clients)
  - `emailTemplate()` is a shared helper — all notifications reuse the same branded layout
  - `notifyMilestoneUnlocked` is not yet integrated — will be called from US-020 success dashboard milestone logic
  - Email `from` address is managed by Insforge — no need to specify sender
---

## 2026-02-23 - US-016
Session: Improvement Opportunities tracker
- Added 4 server actions to `src/actions/trackers.ts`:
  - `getOpportunities()` — fetches all improvement opportunities for user, ordered by created_at desc
  - `createOpportunity(fields)` — creates new opportunity with title, description, priority, category, status
  - `updateOpportunityStatus(id, status)` — inline status change (active/completed/deferred)
  - `deleteOpportunity(id)` — deletes an opportunity by ID
- Created `src/components/trackers/OpportunitiesTracker.tsx` — client component with:
  - "Add Opportunity" button opens Headless UI Dialog modal with form fields
  - Modal form: title, description, priority (dropdown), category (text), status (dropdown)
  - Sortable table: Title (with description), Priority badge, Category, Status dropdown, Delete
  - Filter dropdowns: status (Active/Completed/Deferred/All) and priority (High/Medium/Low/All)
  - Inline status change via `<select>` dropdown on each row with optimistic update
  - Delete button with confirmation dialog ("Are you sure you want to delete this opportunity?")
  - Empty state with icon, message, and "Add Opportunity" CTA
  - Loading skeleton while data fetches
  - Priority badges: High (error red), Medium (warning), Low (sky blue)
- Updated `src/app/(dashboard)/trackers/page.tsx`:
  - Imported `OpportunitiesTracker` component
  - Replaced `OpportunitiesPlaceholder` in Opportunities tab
  - Removed unused `OpportunitiesPlaceholder` function
- Updated AGENTS.md (root, trackers components, actions)
- Files changed: src/actions/trackers.ts, src/components/trackers/OpportunitiesTracker.tsx, src/app/(dashboard)/trackers/page.tsx, AGENTS.md, src/components/trackers/AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Headless UI v2 Dialog modal pattern: `Transition` > `Dialog` with two `TransitionChild` (overlay + panel)
  - Form state reset on modal open: `useEffect` keyed on `open` prop — clean way to reset without lifting state
  - Optimistic status update with revert on failure: update local state first, then await server, refetch on error
  - `PRIORITY_CONFIG` and `STATUS_CONFIG` map patterns keep badge styling consistent across components
  - Removed unused `StatusBadge` — lint caught it in build; inline `<select>` dropdowns used instead for status changes
  - Opportunity CRUD actions follow same `getToken() → getUserId() → insforgeClient.from()` pattern as WAR Battle actions
---

## 2026-02-23 - US-017
Session: Winning Solutions Impact tracker with ROI calculation
- Added 3 server actions to `src/actions/trackers.ts`:
  - `getWinningSolutions()` — fetches all winning solutions for user, ordered by created_at desc
  - `createWinningSolution(fields)` — creates new solution with title, problem, description, metric_type, before/after values, unit, date, notes
  - `deleteWinningSolution(id)` — deletes a solution by ID
- Created `src/components/trackers/ImpactTracker.tsx` — client component with:
  - "Add Solution" modal with fields: title, problem addressed, description, metric type dropdown, unit, before/after values, date, notes
  - Live ROI preview in modal: auto-calculated improvement displayed after entering before/after values
  - ROI formula: ((Before-After)/Before)*100 for reductions (time_saved, cost_reduced), ((After-Before)/Before)*100 for gains (quality_improved, other)
  - Solutions displayed as cards showing title, metric type badge, before→after values, improvement percentage
  - Summary stats bar at top: Total Solutions count + aggregate improvements per metric type with colored backgrounds
  - Delete button with confirmation dialog
  - Empty state with icon, message, and "Add Solution" CTA
  - Loading skeleton while data fetches
- Updated `src/app/(dashboard)/trackers/page.tsx`:
  - Imported `ImpactTracker` component
  - Replaced `ImpactTrackerPlaceholder` in Impact Tracker tab
  - Removed unused `ImpactTrackerPlaceholder` function
- Updated AGENTS.md (root, trackers components, actions)
- Files changed: src/actions/trackers.ts, src/components/trackers/ImpactTracker.tsx, src/app/(dashboard)/trackers/page.tsx, AGENTS.md, src/components/trackers/AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Use `Array.from(set)` instead of `[...set]` spread syntax for Set iteration — TypeScript without `downlevelIteration` flag doesn't support Set spread
  - `METRIC_CONFIG` map pattern: `Record<MetricType, { label, color, bg }>` — consistent metric badge styling across summary stats and solution cards
  - ROI calculation splits into reduction vs gain logic based on metric_type — time_saved and cost_reduced use (before-after)/before, quality_improved and other use (after-before)/before
  - Summary stats `aggregates` uses `reduce()` with a Set to track unique units per metric type — avoids showing mixed units
  - Modal form uses string values for number inputs (`before_value`, `after_value`) — parse to float only on submit to avoid NaN display issues
  - Winning Solutions CRUD follows same `getToken() → getUserId() → insforgeClient.from()` pattern as all other tracker actions
---

## 2026-02-23 - US-018
Session: Success Nuggets library with manual entry
- Added 4 server actions to `src/actions/trackers.ts`:
  - `getSuccessNuggets()` — fetches all success nuggets for user, ordered by created_at desc
  - `createSuccessNugget(fields)` — creates new nugget with achievement_statement, supporting_metrics, talking_points; defaults source to 'manual'
  - `updateSuccessNugget(id, fields)` — updates a nugget's text fields
  - `deleteSuccessNugget(id)` — deletes a nugget by ID
- Created `src/components/trackers/SuccessNuggetsLibrary.tsx` — client component with:
  - "Add Nugget" button opens Headless UI Dialog modal with form fields
  - Modal form: Achievement Statement (textarea), Supporting Metrics (textarea), Talking Points (textarea)
  - Modal supports both add (empty form) and edit (pre-populated) modes via `editNugget` prop
  - Nuggets displayed as cards showing achievement statement, supporting metrics, and talking points
  - Source badge on each card: "Manual" (teal) or "AI Generated" (sky blue) with matching icon
  - Edit and delete action icons on each nugget card header
  - Delete with confirmation dialog
  - Empty state: "Build your success nuggets library for performance reviews and career conversations"
  - Loading skeleton while data fetches
  - Cards in 2-column responsive grid (sm:grid-cols-2)
  - Date shown on each card in "MMM DD, YYYY" format
- Updated `src/app/(dashboard)/trackers/page.tsx`:
  - Imported `SuccessNuggetsLibrary` component
  - Replaced `SuccessNuggetsPlaceholder` in Success Nuggets tab
  - Removed unused `SuccessNuggetsPlaceholder` function
- Updated AGENTS.md (root, trackers components, actions)
- Files changed: src/actions/trackers.ts, src/components/trackers/SuccessNuggetsLibrary.tsx, src/app/(dashboard)/trackers/page.tsx, AGENTS.md, src/components/trackers/AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Edit modal pattern: pass `editNugget` prop + use `useEffect` keyed on `[open, editNugget]` to populate/reset form
  - `createSuccessNugget` sets `source: "manual"` as default, spread `...fields` can override for AI-generated nuggets (US-019)
  - `updateSuccessNugget` only updates text fields — doesn't change source or user_id
  - SuccessNuggetsLibrary uses `handleEdit` which sets `editTarget` state then opens the form modal — reuses same modal for add/edit
  - `NuggetSource` type imported from `@/db/types` but unused in component (only needed in server actions)
  - Nugget cards use `whitespace-pre-line` on metrics/talking points to preserve user line breaks
---

## 2026-02-23 - US-019
Session: AI-powered nugget generation
- Added `insforgeAI.chatCompletion()` helper to `src/lib/insforge.ts` — calls Insforge OpenRouter endpoint `/ai/v1/chat/completions`
- Added 2 server actions to `src/actions/trackers.ts`:
  - `generateSuccessNuggets()` — collects completed opportunities, winning solutions (with metrics), and done WAR Battle sessions; sends context to GPT-4o with career-advancement prompt; returns 3-5 `GeneratedNugget[]` for review
  - `saveGeneratedNuggets(nuggets)` — saves array of AI-generated nuggets with `source='ai_generated'` to success_nuggets table
- Exported `GeneratedNugget` interface from trackers.ts: `{ achievement_statement, supporting_metrics, talking_points }`
- Updated `src/components/trackers/SuccessNuggetsLibrary.tsx`:
  - Added "Generate Nuggets" button (sky blue) next to "Add Nugget" in toolbar and empty state
  - Loading spinner during AI generation
  - Error message display for no tracker data or AI failure
  - Added `AIReviewModal` component — shows generated nuggets in editable textareas with checkboxes to include/exclude
  - User can edit achievement_statement, supporting_metrics, and talking_points before saving
  - "Save X Nuggets" button saves selected nuggets via `saveGeneratedNuggets()`
  - After save, library refreshes to show new AI-generated nuggets with "AI Generated" source badge
- Updated AGENTS.md (root, src/lib, src/actions, src/components/trackers)
- Files changed: src/lib/insforge.ts, src/actions/trackers.ts, src/components/trackers/SuccessNuggetsLibrary.tsx, AGENTS.md, src/lib/AGENTS.md, src/actions/AGENTS.md, src/components/trackers/AGENTS.md
- **Learnings for future iterations:**
  - Insforge OpenRouter AI endpoint: POST `/ai/v1/chat/completions` with OpenAI-compatible schema
  - Default model: `openai/gpt-4o` — passed in body, not as header
  - AI response parsing: ask for "ONLY valid JSON — no markdown, no code fences" to avoid ```json wrappers
  - `GeneratedNugget` is a plain interface (not a DB type) — used as intermediary between AI response and save
  - AIReviewModal uses `selected` boolean array to track which nuggets user wants — cheaper than a Set
  - Guard against empty tracker data before calling AI — show helpful message about what to do first
  - `insforgeAI` doesn't need a token (uses API key only) — unlike REST client calls
---

## 2026-02-23 - US-020
Session: CI Done Right Success Dashboard - milestone cards
- Created `src/actions/success-dashboard.ts` with server actions:
  - `getSuccessDashboardData()` — fetches user profile (name, company), progress records, and milestones
  - `checkAndUnlockMilestones()` — checks if waste_eliminator or ci_consultant milestones should unlock based on completed progress stages, creates/updates milestone records, sends Dana notification via fire-and-forget
- Created `src/app/(dashboard)/success-dashboard/page.tsx` — client component with:
  - Page title: "CI Done Right Success Dashboard" with subtitle "Track your achievements and unlock revenue opportunities"
  - Reuses `JourneyProgressBar` component from dashboard for journey progress visualization
  - 2 milestone cards in responsive grid (1 col mobile, 2 cols desktop)
  - Card 1: "Most Valuable Workplace Waste Eliminator" — blue-to-teal gradient, star icon, requires 4 modules + battle 1 (5 requirements)
  - Card 2: "Certified CI Done Right Consultant" — teal-to-mint gradient, shield icon, requires battles 2 + 3 (2 requirements)
  - Each card: icon, lock/unlock indicator, title, subtitle, requirement text, progress bar, progress count
  - Unlocked milestones show celebration overlay with backdrop blur
  - Milestone check runs on page load, re-fetches data if newly unlocked
  - Loading skeleton while data fetches, error state on failure
- Updated AGENTS.md (root, src/actions)
- Files changed: src/actions/success-dashboard.ts, src/app/(dashboard)/success-dashboard/page.tsx, AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - When defining arrays of `ProgressStage` values, explicitly type as `ProgressStage[]` — TypeScript infers `string[]` otherwise and `Set<ProgressStage>.has()` rejects `string` arguments
  - Milestone unlock uses `checkAndUnlockMilestones()` called from the page's useEffect — fire-and-forget with re-fetch on unlock
  - Milestone configs stored as a typed array of objects for easy iteration — avoids duplicating card rendering logic
  - `completedStages` computed as `Set` from progress records filtered by `status === "completed"` — efficient for `.has()` lookups
  - Success Dashboard reuses `JourneyProgressBar` from dashboard — no need to create a separate progress component
---

## 2026-02-23 - US-021
Session: Success Dashboard - quick stats with circular progress rings
- Created `src/components/dashboard/CircularProgressRing.tsx` — client component with:
  - SVG-based circular progress ring using `strokeDasharray` + `strokeDashoffset` technique
  - Animated fill on mount via `useEffect` + `setTimeout` (starts undrawn, animates to target)
  - Center displays value number with "of {total}" below, label below ring
  - Props: value, total, label, color (Tailwind class), colorHex (for SVG stroke)
  - Ring size: 120x120px, stroke width 10px, background ring in paleGray
- Updated `src/actions/success-dashboard.ts`:
  - Added `sessionsCompleted` field to `SuccessDashboardData` interface
  - Added war_battle_sessions query (status=done count) to `getSuccessDashboardData()`
- Updated `src/app/(dashboard)/success-dashboard/page.tsx`:
  - Imported `CircularProgressRing` component
  - Computed `modulesCompleted` (from module_1..module_4 in progressRecords) and `battlesWon` (from battle_1..battle_3)
  - Added Quick Stats section below milestone cards with 3 rings:
    - Modules Completed (X/4, sky blue #35C0ED)
    - Sessions Completed (X/14, teal #2F90B0)
    - Battles Won (X/3, mint green #9AEBA6)
  - Rings wrapped in white card with `flex-wrap justify-center` for mobile responsiveness
- Updated AGENTS.md (root, dashboard components, actions)
- Files changed: src/components/dashboard/CircularProgressRing.tsx, src/actions/success-dashboard.ts, src/app/(dashboard)/success-dashboard/page.tsx, AGENTS.md, src/components/dashboard/AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - SVG circle progress: use `strokeDasharray={circumference}` + `strokeDashoffset={circumference * (1 - percent)}` for partial arc
  - Rotate SVG `-90deg` to start progress from top (12 o'clock position) instead of 3 o'clock
  - `strokeLinecap="round"` gives rounded ends on the progress arc
  - Both Tailwind class (`color`) and hex value (`colorHex`) needed — class for transition, hex for SVG stroke attribute
  - Modules/battles can be computed from `progressRecords` using `completedStages.has()` — no additional server query needed
  - Sessions count needs separate query on `war_battle_sessions` table (status=done) — not derivable from `user_progress`
---

## 2026-02-23 - US-022
Session: Ingest book content into pgvector for RAG
- Created `src/scripts/ingest-book.ts` — standalone script that:
  - Reads the book markdown file (2059 lines, 15 chapters across 5 parts)
  - Parses chapter boundaries via regex on `## **Chapter N:` headings
  - Strips base64 images, markdown formatting, anchor IDs, and non-content noise
  - Chunks text to ~500 tokens per chunk with 50-token overlap, respecting paragraph boundaries
  - Each chunk tagged with metadata: chapter name, part/section name, approximate page range
  - Generates embeddings via Insforge OpenRouter (`openai/text-embedding-3-small`, 1536 dims)
  - Stores chunks in `book_embeddings` table via Insforge REST API
  - Rate limited: 1s pause every 10 chunks
  - Logs progress: chunk counts, token estimates, success/error totals
- Added `insforgeEmbeddings.create({ input, model? })` to `src/lib/insforge.ts`:
  - Calls Insforge OpenRouter embeddings endpoint (`/ai/v1/embeddings`)
  - Default model: `openai/text-embedding-3-small` (1536 dimensions, matches pgvector schema)
- Run command: `npx tsx --env-file=.env.local src/scripts/ingest-book.ts`
- Created `src/scripts/AGENTS.md` with script documentation
- Updated root `AGENTS.md` with Book Ingestion / RAG section and embeddings client
- Updated `src/lib/AGENTS.md` with embeddings import pattern
- Files changed: src/scripts/ingest-book.ts, src/lib/insforge.ts, src/scripts/AGENTS.md, src/lib/AGENTS.md, AGENTS.md
- **Learnings for future iterations:**
  - Node v22 supports `--env-file` flag natively — avoids `dotenv` dependency for standalone scripts
  - Script uses direct `fetch()` calls (not `insforgeClient`) since it runs outside the Next.js runtime
  - Book markdown has base64 images embedded at the bottom — must strip these before chunking
  - Chapter boundaries found via `## **Chapter N:` regex — no `###` sub-section headings in this book
  - `Prefer: return=minimal` header for inserts when we don't need the response body back
  - pgvector embedding column requires JSON-stringified array for insert via REST API
  - `book_embeddings` RLS: SELECT for authenticated users, INSERT restricted to admin role
  - For RAG queries (US-024): embed question → cosine similarity search → top-5 chunks as context
---

## 2026-02-23 - US-023
Session: CI Professor chat UI
- Created `src/actions/chat.ts` with server actions:
  - `getConversations()` — fetches all conversations for user, ordered by created_at desc
  - `getMessages(conversationId)` — fetches messages for a conversation, ordered by created_at asc
  - `createConversation(title)` — creates a new conversation, returns the created record
  - `sendMessage(conversationId, role, content)` — saves a user or assistant message
  - `updateConversationTitle(conversationId, title)` — updates conversation title
  - `deleteConversation(conversationId)` — deletes conversation (messages cascade via FK)
- Created `src/app/(dashboard)/ci-professor/page.tsx` — client component with:
  - Full chat interface with message bubbles: user (sky blue, right) and AI (white, left)
  - Text input at bottom with send button (navy), Enter to send, Shift+Enter for newline
  - Auto-resizing textarea (max 150px height)
  - "New Chat" button in header to start fresh conversation
  - Empty state with 3 starter questions — clicking sends as first message
  - Chat history dropdown in header with conversation list, active highlighting, and inline delete confirmation
  - Typing indicator (3 animated dots) while waiting for AI response
  - Optimistic message display — user message appears immediately
  - Conversation auto-created on first message with truncated title (50 chars)
  - Full-height layout: `h-[calc(100vh-128px)]`
  - Professor avatar (navy circle, graduation cap) and user avatar (mint green, "U")
  - Timestamps on each message bubble
  - Placeholder AI response for UI-only phase — actual RAG integration in US-024
- Updated AGENTS.md (root, src/actions) with CI Professor and Chat Actions sections
- Files changed: src/actions/chat.ts, src/app/(dashboard)/ci-professor/page.tsx, AGENTS.md, src/actions/AGENTS.md
- **Learnings for future iterations:**
  - Insforge REST API insert returns array by default with `Prefer: return=representation` — use `Array.isArray(data) ? data[0] : data` to extract single record
  - Chat page uses `h-[calc(100vh-128px)]` to fill available space (64px header + 64px app header = 128px)
  - `useRef` for both messagesEnd (auto-scroll) and textarea (focus management after send)
  - Textarea auto-resize: set `height: auto` then `height: scrollHeight` on change, cap at 150px
  - History dropdown uses `fixed inset-0` invisible overlay to close on outside click — lightweight alternative to Headless UI Dialog for simple dropdowns
  - Delete confirmation uses inline pattern (Delete/Cancel buttons) instead of a separate modal — less overhead for simple confirmations
  - Conversation created lazily on first message send — avoids empty conversation records
  - Placeholder AI response in US-023 will be replaced by actual RAG pipeline in US-024
---

## 2026-02-23 - US-024
Session: Integrate RAG pipeline and streaming AI responses for CI Professor
- Created `src/app/api/chat/route.ts` — POST endpoint with full RAG pipeline:
  - Authenticates user via `access_token` cookie + `insforgeAuth.getUser()`
  - Embeds user question via `insforgeEmbeddings.create()` (openai/text-embedding-3-small, 1536 dims)
  - Queries pgvector for top-5 similar book chunks via RPC `match_book_embeddings`
  - Fallback direct pgvector query if RPC function not available
  - Builds LLM messages: system prompt (CI Professor personality) + book context + last 10 history messages + user question
  - Streams GPT-4o response via Insforge OpenRouter `/ai/v1/chat/completions` with `stream: true`
  - TransformStream extracts `choices[0].delta.content` from OpenAI-compatible SSE stream
  - Returns cleaned SSE stream with `data: {"content": "token"}\n\n` format
- Created `src/db/migrations/004_match_book_embeddings_fn.sql` — RPC function for semantic search:
  - `match_book_embeddings(query_embedding vector(1536), match_count int)` returns chunks with similarity scores
  - Uses `1 - (embedding <=> query_embedding)` for cosine similarity
- Updated `src/app/(dashboard)/ci-professor/page.tsx`:
  - Replaced placeholder AI response with streaming fetch to `/api/chat`
  - Added `isStreaming` and `streamingContent` state for live token display
  - Streaming message bubble with blinking cursor indicator during token arrival
  - Error state display below messages if AI call fails
  - SSE stream parsed via ReadableStream reader with buffer for split chunks
  - After streaming completes, full content saved to messages table via `sendMessage()`
  - Falls back to optimistic message display if save fails
  - Conversation history passed to API for multi-turn context
- Updated AGENTS.md files: root, src/app/api/chat/, src/actions/, src/db/
- Files changed: src/app/api/chat/route.ts, src/app/api/chat/AGENTS.md, src/db/migrations/004_match_book_embeddings_fn.sql, src/app/(dashboard)/ci-professor/page.tsx, AGENTS.md, src/actions/AGENTS.md, src/db/AGENTS.md, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Next.js Route Handlers support `cookies()` from `next/headers` — same as server actions
  - Streaming requires both server-side TransformStream (to process upstream SSE) and client-side ReadableStream reader (to parse downstream SSE)
  - SSE chunks can be split across multiple reads — must use a buffer pattern: `buffer += decode, split on \n, keep last incomplete line`
  - `insforgeEmbeddings` and `insforgeAI` use API key only (no JWT needed) — unlike REST client which requires user token
  - RPC functions called via POST to `/rest/v1/rpc/{function_name}` — input as JSON body
  - `stream: true` in OpenAI-compatible API returns SSE with `data: {"choices":[{"delta":{"content":"..."}}]}` format
  - Off-topic redirect message defined in system prompt — no client-side filtering needed
---

## 2026-02-23 - US-025
Session: Settings and profile page
- Created `src/actions/settings.ts` with server actions:
  - `getProfileData()` — fetches user profile fields for settings page (name, email, company, industry, role, miestro status, notification prefs)
  - `updateProfile(fields)` — updates name, company, industry, role in users table
  - `changePassword(currentPassword, newPassword)` — verifies current password via login, then updates
- Created `src/app/(dashboard)/settings/page.tsx` — client component with:
  - Profile section: editable fields for name, company, industry (dropdown), role/title. Email read-only.
  - "Save Changes" button persists profile updates with success toast (3s auto-dismiss)
  - Notification preferences: toggle switches for milestone notifications and weekly progress digest
  - Miestro account section: displays linked/unlinked status with "Link Account" link (placeholder URL)
  - Security section: "Change Password" button opens Headless UI Dialog modal
  - Password modal: current password, new password (with inline validation), confirm password, Update/Cancel buttons
  - Password validation: 8+ chars, 1 uppercase, 1 number — inline check/x icons (reuses signup pattern)
  - Loading skeleton while profile data fetches
  - Form uses design system styling: md border radius inputs, navy primary buttons
- Updated AGENTS.md (root, src/actions)
- Files changed: src/actions/settings.ts, src/app/(dashboard)/settings/page.tsx, AGENTS.md, src/actions/AGENTS.md, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - `changePassword` verifies current password by calling `insforgeAuth.login()` — if login succeeds, current password is correct
  - Email field displayed as read-only with `readOnly` attribute and `bg-offWhite cursor-not-allowed text-charcoal/60` styling
  - Toggle switch pattern: `role="switch"` + `aria-checked` button with `translate-x-6`/`translate-x-1` for knob position
  - Notification toggles are UI-only for MVP — not persisted to database (no notification_preferences column yet)
  - Password modal state resets on open via `useEffect` keyed on `passwordModalOpen`
  - Success toast uses `setTimeout(() => setSuccessMessage(""), 3000)` for auto-dismiss
---
